{
  "name": "peerlocal-sg",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ -sg",
    "create-local": "graph create --node http://localhost:8020/ peerlocal-sg",
    "remove-local": "graph remove --node http://localhost:8020/ peerlocal-sg",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 peerlocal-sg",
    "test": "graph test",
    "deploy-hosted:mumbai": "graph deploy --product hosted-service krisgrm/peerlocal-sg-mumbai"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.49.0",
    "@graphprotocol/graph-ts": "^0.30.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.5.2",
    "prettier": "^2.8.8"
  }
}
